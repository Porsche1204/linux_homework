#include<stdio.h>
#include<stdlib.h>
#include<sys/stat.h>
#include<string.h>

int main(int argc, char *argv[])
{
	char *cmd[4];
	if(argc != 2 || argc > 2)
	{
		printf("Error! The format is not right! \n");
		exit(EXIT_FAILURE);
	}
	return 0;
}
